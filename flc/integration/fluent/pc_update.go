// This source code is licensed under the Apache 2.0 license found
// in the LICENSE file in the root directory of this source tree.

// Code generated by ent, DO NOT EDIT.

package fluent

import (
	"context"
	"errors"
	"fmt"

	"github.com/usalko/fluent/dialect/sql"
	"github.com/usalko/fluent/dialect/sql/sqlgraph"
	"github.com/usalko/fluent/flc/integration/fluent/pc"
	"github.com/usalko/fluent/flc/integration/fluent/predicate"
	"github.com/usalko/fluent/schema/field"
)

// PCUpdate is the builder for updating PC entities.
type PCUpdate struct {
	config
	hooks     []Hook
	mutation  *PCMutation
	modifiers []func(*sql.UpdateBuilder)
}

// Where appends a list predicates to the PCUpdate builder.
func (_pc *PCUpdate) Where(ps ...predicate.PC) *PCUpdate {
	_pc.mutation.Where(ps...)
	return _pc
}

// Mutation returns the PCMutation object of the builder.
func (_pc *PCUpdate) Mutation() *PCMutation {
	return _pc.mutation
}

// Save executes the query and returns the number of nodes affected by the update operation.
func (_pc *PCUpdate) Save(ctx context.Context) (int, error) {
	return withHooks(ctx, _pc.sqlSave, _pc.mutation, _pc.hooks)
}

// SaveX is like Save, but panics if an error occurs.
func (_pc *PCUpdate) SaveX(ctx context.Context) int {
	affected, err := _pc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return affected
}

// Exec executes the query.
func (_pc *PCUpdate) Exec(ctx context.Context) error {
	_, err := _pc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_pc *PCUpdate) ExecX(ctx context.Context) {
	if err := _pc.Exec(ctx); err != nil {
		panic(err)
	}
}

// Modify adds a statement modifier for attaching custom logic to the UPDATE statement.
func (pc *PCUpdate) Modify(modifiers ...func(u *sql.UpdateBuilder)) *PCUpdate {
	pc.modifiers = append(pc.modifiers, modifiers...)
	return pc
}

func (_pc *PCUpdate) sqlSave(ctx context.Context) (n int, err error) {
	_spec := sqlgraph.NewUpdateSpec(pc.Table, pc.Columns, sqlgraph.NewFieldSpec(pc.FieldID, field.TypeInt))
	if ps := _pc.mutation.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	_spec.AddModifiers(pc.modifiers...)
	if n, err = sqlgraph.UpdateNodes(ctx, _pc.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{pc.Label}
		} else if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return 0, err
	}
	_pc.mutation.done = true
	return n, nil
}

// PCUpdateOne is the builder for updating a single PC entity.
type PCUpdateOne struct {
	config
	fields    []string
	hooks     []Hook
	mutation  *PCMutation
	modifiers []func(*sql.UpdateBuilder)
}

// Mutation returns the PCMutation object of the builder.
func (po *PCUpdateOne) Mutation() *PCMutation {
	return po.mutation
}

// Where appends a list predicates to the PCUpdate builder.
func (po *PCUpdateOne) Where(ps ...predicate.PC) *PCUpdateOne {
	po.mutation.Where(ps...)
	return po
}

// Select allows selecting one or more fields (columns) of the returned entity.
// The default is selecting all fields defined in the entity schema.
func (po *PCUpdateOne) Select(field string, fields ...string) *PCUpdateOne {
	po.fields = append([]string{field}, fields...)
	return po
}

// Save executes the query and returns the updated PC entity.
func (po *PCUpdateOne) Save(ctx context.Context) (*PC, error) {
	return withHooks(ctx, po.sqlSave, po.mutation, po.hooks)
}

// SaveX is like Save, but panics if an error occurs.
func (po *PCUpdateOne) SaveX(ctx context.Context) *PC {
	node, err := po.Save(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// Exec executes the query on the entity.
func (po *PCUpdateOne) Exec(ctx context.Context) error {
	_, err := po.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (po *PCUpdateOne) ExecX(ctx context.Context) {
	if err := po.Exec(ctx); err != nil {
		panic(err)
	}
}

// Modify adds a statement modifier for attaching custom logic to the UPDATE statement.
func (po *PCUpdateOne) Modify(modifiers ...func(u *sql.UpdateBuilder)) *PCUpdateOne {
	po.modifiers = append(po.modifiers, modifiers...)
	return po
}

func (po *PCUpdateOne) sqlSave(ctx context.Context) (_node *PC, err error) {
	_spec := sqlgraph.NewUpdateSpec(pc.Table, pc.Columns, sqlgraph.NewFieldSpec(pc.FieldID, field.TypeInt))
	id, ok := po.mutation.ID()
	if !ok {
		return nil, &ValidationError{Name: "id", err: errors.New(`fluent: missing "PC.id" for update`)}
	}
	_spec.Node.ID.Value = id
	if fields := po.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, pc.FieldID)
		for _, f := range fields {
			if !pc.ValidColumn(f) {
				return nil, &ValidationError{Name: f, err: fmt.Errorf("fluent: invalid field %q for query", f)}
			}
			if f != pc.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, f)
			}
		}
	}
	if ps := po.mutation.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	_spec.AddModifiers(po.modifiers...)
	_node = &PC{config: po.config}
	_spec.Assign = _node.assignValues
	_spec.ScanValues = _node.scanValues
	if err = sqlgraph.UpdateNode(ctx, po.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{pc.Label}
		} else if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	po.mutation.done = true
	return _node, nil
}
